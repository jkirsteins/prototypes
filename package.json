{
  "name": "escape-castle",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile:ink": "inkjs-compiler src/ink/coffin.ink -o src/ink/coffin.json",
    "dev": "vite --host 127.0.0.1",
    "build": "npm run compile:ink && tsc && vite build",
    "test": "npm run compile:ink && vitest run"
  },
  "dependencies": {
    "inkjs": "^2.4.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  }
}
